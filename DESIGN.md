# Design.md — 자소서 대리작성 서비스 디자인 시스템

## 0. 컨셉 한 줄 요약
**"초안에 도장을 찍다."**
정리되지 않은 경험(STAR)이 다듬어져 정식 제출 문서가 되는 과정을 시각적으로 표현한다. 평소엔 흑백·그레이의 차분한 종이 위에서 작업하다가, 문항이 "완성"되는 순간에만 포인트 컬러가 등장한다 — 마치 공문서에 찍는 도장(직인)처럼, 색은 장식이 아니라 **확정/완료의 의미**로만 쓰인다.

---

## 1. 컬러 팔레트

| 이름 | Hex | 용도 |
|---|---|---|
| Ink (잉크) | `#1A1B20` | 본문 텍스트, 헤딩 (완전한 블랙 대신 살짝 톤 다운된 무채색 — 화면에서 눈부심 방지) |
| Paper (종이) | `#FAFAF8` | 배경 (완전한 화이트 대신 미세하게 따뜻한 오프화이트 — 종이 질감) |
| Fog (그레이-중간) | `#8A8D93` | 보조 텍스트, placeholder, 비활성 라벨 |
| Line (그레이-연함) | `#E4E4E1` | 구분선, 카드 테두리, input border |
| Panel (그레이-패널) | `#F0F0EE` | 카드/섹션 배경, 코드블록·태그 배경 |
| **Seal Blue (도장 블루)** | **`#2F3EA8`** | **포인트 컬러 — 완료 상태, 주요 CTA, 활성 태그, 생성 완료 마크에만 사용** |

**사용 원칙**
- Seal Blue는 화면 전체에서 "가장 중요한 상태 변화"에만 등장한다: 생성 완료 뱃지, 주요 액션 버튼(생성/복사), 선택된 경험 태그, 글자수 통과 표시.
- 대기 상태·입력 중·비활성 요소는 철저히 무채색으로 유지해, Seal Blue가 나타났을 때 "일이 끝났다"는 신호로 즉시 인지되게 한다.
- 경고/에러(글자수 초과 등)는 Seal Blue를 쓰지 않고 별도의 절제된 톤(`#B3261E` 계열, 최소 사용)으로 분리한다.

---

## 2. 타이포그래피

| 역할 | 폰트 | 비고 |
|---|---|---|
| Display (제목) | **Pretendard** (Bold/SemiBold), 한글 지원 우수, 각진 인상 | 페이지 타이틀, 섹션 헤딩 |
| Body (본문) | **Pretendard** (Regular/Medium) | 자소서 본문, 설명 텍스트 |
| Utility/Data (라벨·숫자·태그·글자수) | **JetBrains Mono** | 글자수 카운터, 경험 태그, 문항 번호, "used_experience" 라벨 등 — 개발자 정체성을 은근히 드러내는 요소 |

- 자소서 생성 결과 텍스트는 Body(Pretendard)로, **글자수 카운터·경험 태그·상태 라벨은 전부 모노스페이스**로 분리한다. 이 대비가 "내용(사람이 쓴 글)"과 "메타데이터(시스템이 관리하는 값)"를 시각적으로 구분해준다 — 코드 에디터에서 주석과 코드가 구분되는 방식과 유사.
- 타입 스케일: 제목 28/22/18px (SemiBold), 본문 15px (Regular, line-height 1.7 — 자소서는 줄글이 길어 가독성 우선), 라벨/모노 12–13px.

---

## 3. 레이아웃 컨셉

**"좌: 재료(경험) / 우: 결과(자소서)" — 편집기 같은 2단 구조**

경험을 코드/재료로, 생성된 자소서를 컴파일된 결과물로 은유한다. 자소서 생성 페이지는 좌측에 입력(공고 정보 + 경험 선택), 우측에 결과가 실시간으로 쌓이는 구조.

```
┌─────────────────────────────┬───────────────────────────────┐
│  공고 정보 입력                │  생성 결과 (문항별)              │
│  ─────────────────           │  ─────────────────             │
│  회사/직무                     │  Q1. 지원 동기         [487/500]│
│  지원자격 / 우대사항             │  ─────────────────────         │
│                              │  (생성된 본문 텍스트...)          │
│  문항 목록                     │                                │
│  □ Q1  [500자]                │  활용 경험: [프로젝트A] [공모전B] │ ← Seal Blue 태그
│  □ Q2  [800자]                │  [재생성]  [복사하기 ✓ 완료]      │ ← 완료 시 Seal Blue
│                              │                                │
│  경험 선택                     │  Q2. 성장 과정         [준비중]  │
│  ☑ 프로젝트A   ☐ 공모전B         │                                │
│  ☐ 동아리C     ☑ 자격증D         │                                │
│                              │                                │
│  [ 자소서 생성 ]  ← Seal Blue    │                                │
└─────────────────────────────┴───────────────────────────────┘
```

- 경험 목록/카드형 페이지(`/experiences`)는 그리드 카드 형태, 카테고리는 색이 아니라 **모노 라벨 텍스트**(예: `PROJECT`, `CONTEST`)로만 구분 — 카테고리마다 색을 다르게 쓰지 않고 무채색 내 명도 차이로만 구분해 팔레트 원칙을 지킨다.
- 여백은 넉넉하게 (섹션 간 48px+), 테두리는 1px hairline (`Line` 컬러), border-radius는 4–6px 정도로 절제 (완전 각짐도, 과한 둥긂도 아님 — "정식 서류" 느낌에 맞게 딱딱하지 않은 실무형 UI).

---

## 4. 시그니처 요소: "완료 스탬프"

문항의 자소서 생성이 끝나면, 결과 카드 우측 상단에 **작은 원형 마크**가 Seal Blue로 나타난다 (도장이 찍히듯 살짝 스케일 애니메이션 0.9→1.0, 150ms, 과하지 않게 1회만). 이 마크는:
- 평소엔 존재하지 않다가 완료 시에만 등장 → 화면 전체에서 "지금 무엇이 끝났는지"를 한눈에 알려주는 유일한 색 신호
- 재생성 시 사라졌다가 다시 완료되면 재등장
- 아이콘은 화려한 인장 대신 심플한 원 + 체크 형태로, "관공서 도장"을 절제된 UI 언어로 재해석

이 스탬프 하나가 이 서비스에서 유일하게 허용되는 "장식적 모먼트"이며, 나머지 UI는 철저히 조용하고 절제된 흑백 톤을 유지한다.

---

## 5. 컴포넌트 톤

- **버튼(Primary)**: Seal Blue 배경, 흰 텍스트 — "생성", "복사하기" 등 핵심 액션에만 사용
- **버튼(Secondary)**: 무채색 아웃라인 (Ink 텍스트 + Line 테두리) — "재생성", "수정" 등
- **태그/뱃지**: 기본은 Panel 배경 + Fog 텍스트(무채색), 선택/활용된 경험 태그만 Seal Blue 아웃라인 + Seal Blue 텍스트로 강조
- **입력창**: Line 테두리, 포커스 시에만 Seal Blue 1.5px 테두리로 전환 (포커스 상태도 "지금 활성화된 곳"이라는 확정적 신호이므로 컬러 사용 정당함)
- **글자수 카운터**: 기본 Fog, 제한 내 도달 시 Seal Blue, 초과 시 절제된 경고색

---

## 6. 이 팔레트를 고른 이유 (근거 정리)

일반적인 "깔끔한 모노톤 + 포인트 컬러"라고 하면 하늘색이나 민트 등 무난한 색을 고르기 쉽지만, 이 서비스의 본질 — *경험이라는 원재료가 다듬어져 회사에 "정식 제출"되는 문서가 된다* — 에서 출발해 색을 정했다. 한국에서 정식 서류에 찍는 도장·직인의 남색은 "이제 이건 공식적인 것이다"라는 의미를 담고 있고, 이는 이 서비스가 사용자에게 주고 싶은 감각(초안 완성 = 제출 가능한 상태)과 정확히 맞닿아 있다. 동시에 인디고 계열은 IT/테크 제품에서 신뢰·논리를 상징하는 색이기도 해, 대상 사용자(IT 직무 지원자)의 맥락과도 자연스럽게 겹친다.
