# PRD: IT 직무 자소서(자기소개서) 생성 서비스

## 0. 문서 정보
- 작성일: 2026-08-25
- 버전: v0.1 (초안)
- 작성 목적: 개인 프로젝트로 시작하되, 추후 회원가입 기반 다중 사용자 서비스로 확장 가능한 구조 설계

---

## 1. 프로젝트 개요

### 1.1 한 줄 정의
IT 직무 취업 준비생이 자신의 **경험(스펙) 데이터베이스**를 미리 구축해두면, **채용공고 정보(자격요건/우대사항)** 를 입력했을 때 Gemini API가 가장 적합한 경험을 자동으로 골라 STAR 기법 기반의 자소서를 생성해주는 웹 서비스.

### 1.2 배경 / 문제 정의
- 자소서 작성 시 매번 자신의 경험을 다시 정리해서 쓰는 것은 반복적이고 비효율적임.
- 공고마다 요구하는 역량/키워드가 달라, 같은 경험이라도 강조점을 다르게 써야 함.
- 경험을 미리 STAR 형식으로 구조화해두면, AI가 공고 키워드와 매칭해 적합한 경험을 골라 자동으로 문맥에 맞게 재구성할 수 있음.

### 1.3 핵심 가치 제안
1. 경험을 한 번만 잘 정리해두면 여러 공고에 재사용 가능
2. 공고 키워드 ↔ 경험 자동 매칭으로 "이 공고에 가장 잘 맞는 경험이 뭐지?" 고민 시간 단축
3. STAR 기법 기반 구조화로 논리적인 자소서 초안 확보

---

## 2. 대상 사용자
- **1차 타깃**: 개발자를 준비하는 자기 자신 (개인용 도구로 시작)
- **2차 확장**: 회원가입 후 각자의 경험 DB를 관리하는 다중 사용자 서비스
- Google/이메일 로그인 기반 인증 지원 (Supabase Auth)

> MVP는 로그인 없이 단일 사용자로 먼저 동작하게 만들고, 이후 Supabase Auth를 얹어 멀티유저로 확장하는 순서를 권장합니다.

---

## 3. 기술 스택 (제안)

| 영역 | 기술 | 이유 |
|---|---|---|
| 프레임워크 | Next.js (App Router) | 프론트+백엔드(API Route) 한 번에 처리, 배포 쉬움 (Vercel) |
| DB / Auth | Supabase (PostgreSQL) | 무료 티어로 시작 가능, 이메일/구글 로그인 내장, RLS로 사용자별 데이터 격리 |
| AI 생성 | Google Gemini API | 자소서 텍스트 생성 + 키워드 추출 |
| 스타일링 | Tailwind CSS | 빠른 UI 개발 |
| 배포 | Vercel | Next.js와 궁합, 무료 배포 |

**로컬 저장 폴백**: Supabase 연동이 부담스러울 경우, MVP 1단계는 브라우저 `localStorage` 기반으로 먼저 구현하고, 2단계에서 Supabase로 마이그레이션하는 것도 가능. (아래 로드맵 참고)

---

## 4. 정보 구조 (사이트맵)

```
/ (홈 - 대시보드)
├── /experiences        경험 목록 페이지
│   ├── /experiences/new     경험 추가
│   └── /experiences/[id]    경험 상세/수정
├── /cover-letters       자소서 생성 페이지
│   ├── /cover-letters/new   신규 자소서 생성
│   └── /cover-letters/[id]  생성된 자소서 상세/수정/재생성
├── /login, /signup      인증 (2단계 확장 시)
```

---

## 5. 핵심 기능

### 5.1 경험 페이지 (`/experiences`)

#### 5.1.1 경험 목록
- 카테고리별 필터링: 프로젝트 / 공모전 / 강의(교육) / 동아리(대외활동) / 자격증 / 인턴/경력 / 기타
- 카드 형태로 제목, 카테고리, 기간, 한줄요약 노출
- 검색 기능 (제목/키워드)

#### 5.1.2 경험 추가/수정 - 입력 항목
| 필드 | 설명 | 필수여부 |
|---|---|---|
| 카테고리 | 프로젝트/공모전/강의/동아리/자격증/인턴/기타 | 필수 |
| 제목 | 예: "실시간 채팅 서비스 개발" | 필수 |
| 기간 | 시작~종료 (년/월) | 선택 |
| 소속/기관 | 회사, 학교, 동아리명 등 | 선택 |
| 사용 기술/툴 | 태그 형식 (예: React, AWS, Python) | 선택 |
| **S (Situation)** | 상황: 어떤 배경/문제 상황이었는지 | 필수 |
| **T (Task)** | 과제: 본인에게 주어진 역할/목표 | 필수 |
| **A (Action)** | 행동: 구체적으로 무엇을 했는지 | 필수 |
| **R (Result)** | 결과: 정량/정성적 성과 | 필수 |
| 키워드(태그) | 이 경험을 대표하는 역량 키워드 (예: "협업", "문제해결", "성능개선") — 자동 추출 + 수동 편집 가능 | 선택 |

> **질문**: 카테고리별로 STAR 외에 추가로 필요한 항목이 있을까요? (예: 자격증은 발급기관/취득일, 프로젝트는 팀 구성 인원/본인 역할 비중 등) 우선은 공통 STAR 양식으로 통일하고, 필요시 카테고리별 부가 필드를 추가하는 걸로 가정하고 진행하겠습니다.

### 5.2 자소서 생성 페이지 (`/cover-letters`)

#### 5.2.1 입력 항목
1. **회사/공고 정보**
   - 회사명, 직무명
   - 지원자격 (텍스트 붙여넣기)
   - 우대사항 (텍스트 붙여넣기)
   - 채용공고 전문 (선택, 붙여넣으면 키워드 추출 정확도 상승)
2. **문항 입력** (사용자가 직접 입력, 여러 개 추가 가능)
   - 문항 내용 (예: "지원 동기를 서술하시오")
   - 글자수 제한 (예: 500자, 1000자)
   - 문항마다 "+  추가" 버튼으로 여러 개 등록
3. **경험 선택**
   - 경험 페이지에 등록된 경험 목록에서 체크박스로 선택
   - "AI 자동 추천" 옵션: 공고 키워드와 매칭도가 높은 경험을 AI가 우선 추천 (사용자가 최종 선택/해제 가능)
4. **생성 버튼**
   - 클릭 시 아래 5.3 로직 수행 → 문항별 자소서 초안 생성

#### 5.2.2 결과 화면
- 문항별로 생성된 자소서 텍스트 표시 (글자수 카운터 포함, 제한 초과 시 경고)
- 어떤 경험을 기반으로 작성됐는지 하단에 태그로 표시 (예: "활용된 경험: OO 프로젝트, OO 공모전")
- 재생성 버튼 (문항 단위 / 전체 단위)
- 직접 수정(편집) 가능한 텍스트 영역
- **"복사하기" 버튼**: 문항별 텍스트를 클립보드로 복사 (파일 다운로드 기능은 제공하지 않음 — 사용자는 복사 후 실제 채용 사이트 자소서 입력창에 붙여넣어 사용)
  - 본 서비스는 완성본 제출용이 아닌 **초안 대리작성**에 목적이 있으므로, 생성된 텍스트는 어디까지나 사용자가 직접 다듬어 사용하는 것을 전제로 함

### 5.3 AI 처리 로직 (Gemini API — 자소서 1건 생성당 단일 호출 원칙)

**설계 원칙**: 비용/속도 관리를 위해 "생성" 버튼 1회 클릭 = **Gemini API 호출 1회**로 제한한다. 키워드 추출, 경험 매칭, 문항별 자소서 작성을 모두 하나의 프롬프트/응답 사이클 안에서 처리한다. (기존 초안의 Step 1~3 개별 호출 방식은 폐기)

**사전 처리 (API 호출 없이 로컬에서 수행)**
- 사용자가 "경험 선택"에서 고른 경험들(혹은 AI 자동 추천을 켠 경우 전체 등록 경험)을 그대로 프롬프트 컨텍스트에 포함할 데이터로 정리
- 경험이 너무 많을 경우, 사용자가 명시적으로 선택한 것 위주로 컨텍스트 크기를 관리 (토큰 절약)

**단일 API 호출 구성**
- **입력(프롬프트)에 한 번에 포함되는 정보**
  1. 회사명/직무명, 지원자격, 우대사항, 공고 전문(있는 경우)
  2. 선택된 경험 목록 전체 (카테고리, 제목, 기간, 기술스택, STAR 4항목, 키워드)
  3. 생성해야 할 문항 목록 전체 (문항 내용 + 문항별 글자수 제한)
- **AI에게 요청하는 작업 (한 번의 응답 안에서 모두 수행)**
  1. 공고에서 핵심 역량 키워드를 스스로 파악
  2. 문항별로 가장 적합한 경험을 **1~2개** 선택(매칭) — 문항당 활용 경험 개수를 1~2개로 제한하도록 프롬프트에 명시 (여러 경험을 나열식으로 욱여넣지 않고, 하나의 이야기로 깊이 있게 풀어내기 위함)
  3. 선택한 경험을 문항 의도에 맞게 재구성하여 문항별 답안 작성 (경험을 그대로 나열하지 않고 자연스러운 문장으로)
  4. 각 문항의 글자수 제한을 지켜서 작성
- **출력 형식**: JSON 구조로 응답 요청 (문항 ID별 `answer`, `used_experience_ids`, `used_keywords` 포함) → 프론트에서 그대로 파싱해 문항별 결과와 "활용된 경험" 태그를 렌더링

```json
// Gemini 응답 예시 형식
{
  "extracted_keywords": ["React", "협업", "AWS", "문제해결"],
  "answers": [
    {
      "question_id": "q1",
      "answer": "...",
      "used_experience_ids": ["exp_3", "exp_7"],
      "char_count": 487
    }
  ]
}
```

**재생성(재요청) 시 처리 — 전체/문항 단위 재생성만 지원**
- "전체 재생성": 모든 문항을 처음부터 다시 생성, API 1회 재호출
- "문항 단위 재생성": 해당 문항 1개를 처음부터 다시 생성, API 1회 호출
- **"이 부분만 다르게 수정" 같은 부분 편집/부분 재생성 기능은 제공하지 않는다.** 본 서비스는 완성도 높은 최종본이 아니라 빠른 초안(대리작성)을 제공하는 데 목적이 있으므로, 세부 표현 수정은 사용자가 결과 텍스트를 직접 편집(복사 전 텍스트 영역에서 수정)하는 것으로 처리한다.
- 글자수 초과 등으로 인한 자동 재시도(retry)는 지양하고, 프롬프트 내에 글자수 제한을 강하게 명시해 1회 호출 안에서 지켜지도록 설계. 부득이한 경우 사용자가 "재생성" 버튼을 눌러 명시적으로 재호출하게 한다.

> 참고: "키워드 추출"은 별도 화면 기능이 아니라 자소서 생성 호출 1회의 결과물(`extracted_keywords`) 중 하나로 노출되는 방식으로 변경되었습니다.

---

## 6. 데이터 모델 (초안)

```sql
-- 사용자 (Supabase Auth 기본 제공 테이블 활용)

-- 경험
experiences (
  id, user_id,
  category enum('project','contest','course','club','certificate','career','etc'),
  title, organization, start_date, end_date,
  tech_stack text[],
  situation text, task text, action text, result text,
  keywords text[],
  created_at, updated_at
)

-- 자소서 (공고 단위)
cover_letters (
  id, user_id,
  company_name, job_title,
  qualification text,   -- 지원자격
  preference text,      -- 우대사항
  job_posting_raw text,  -- 공고 전문(선택)
  extracted_keywords text[],
  created_at, updated_at
)

-- 자소서 문항
cover_letter_questions (
  id, cover_letter_id,
  question text,
  char_limit int,
  generated_answer text,
  used_experience_ids uuid[],
  created_at, updated_at
)
```

---

## 7. 화면 흐름 (User Flow)

1. (최초 1회) 경험 페이지에서 자신의 스펙을 STAR 형식으로 등록
2. 자소서 생성 페이지 진입 → 회사/공고 정보 입력
3. 문항 추가 (여러 개 가능, 글자수 제한 설정)
4. 경험 선택 (수동 선택 또는 AI 자동 추천 켜기)
5. "생성" 버튼 클릭
6. 문항별 자소서 초안 확인 → 필요시 재생성/수정
7. 저장 및 다운로드

---

## 8. 비기능 요구사항
- **Gemini API 호출 최소화**: "생성"/"재생성" 액션 1건당 API 호출 1회 원칙. 키워드 추출·경험 매칭·문항별 답안 작성을 모두 단일 호출 안에서 처리 (비용/속도 관리 목적)
- Gemini API 키는 서버(API Route)에서만 사용, 클라이언트 노출 금지
- 응답 속도: 문항당 생성 시간 5~15초 이내 목표 (로딩 상태 UI 필수)
- 개인정보(경험 데이터)는 사용자별로 분리 저장 (Supabase RLS 정책 적용 예정)
- 반응형 UI (모바일에서도 사용 가능하도록)

---

## 9. MVP 범위 및 로드맵

### Phase 1 (MVP - 개인용, 로그인 없음)
- [ ] 경험 등록/수정/삭제 (localStorage 또는 Supabase, 단일 사용자)
- [ ] 자소서 생성 페이지: 공고 정보 입력 + 문항 추가 + 경험 수동 선택
- [ ] Gemini API 연동 - 단일 호출로 키워드 추출+경험 매칭+문항별 자소서 생성 동시 처리 (JSON 응답)
- [ ] 생성 결과 확인/편집/복사(클립보드) — 파일 다운로드 기능 없음

### Phase 2 (매칭 고도화)
- [ ] AI 자동 경험 추천 (키워드 매칭 점수화)

### Phase 3 (멀티유저화)
- [ ] Supabase Auth 연동 (이메일/구글 로그인)
- [ ] 사용자별 데이터 분리 (RLS)
- [ ] 회원가입/로그인 UI

### Phase 4 (확장, 선택)
- [ ] 자소서 히스토리 버전 관리
- [ ] 여러 공고 동시 비교/관리

---

## 10. 확정된 결정 사항 (v0.2 반영)

- ✅ 문항 1개당 활용 경험 개수: **1~2개**로 제한
- ✅ 부분 재생성("이 부분만 다르게") 기능: **미지원**. 전체/문항 단위 재생성만 제공하며, 세부 수정은 사용자가 직접 편집
- ✅ 다운로드 기능: **미지원**. "복사하기" 버튼으로 클립보드 복사 후 사용자가 직접 채용 사이트에 붙여넣는 방식 (본 서비스는 초안 대리작성이 목적)
- ✅ Gemini API 호출: 자소서 생성/재생성 액션 1건당 1회 호출 원칙

## 11. 미해결 질문 (추가로 확인이 필요한 부분)

1. 카테고리별로 STAR 외 부가 입력 항목이 필요한지 (예: 자격증의 발급기관/등급)
2. Phase 1을 localStorage로 시작할지, 처음부터 Supabase로 갈지

위 질문들은 답변 주시는 대로 문서에 반영하겠습니다.
